include conf.d/peresvet/upstreams/*.conf;

server {
    listen ${NGINX_HTTP_PORT};
    server_name 0.0.0.0 localhost ${SRV_NAME};

    client_max_body_size 15m;

    set $proto http;

    resolver 127.0.0.11 valid=5s;
    include conf.d/peresvet/locations/*.conf
}

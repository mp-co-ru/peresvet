В джсонах

1) rabbitmq заменил на localhost
2) все в один файл (кроме тестов и тех приложений, у которых не было json-ов)
3) в каждом приложении добавил соотвтетствующий спецификатор в название
------------------------------
В докер файле

1) все ниже # для каждого сервиса создадим свой каталог ------------------- 
скопировал попросту